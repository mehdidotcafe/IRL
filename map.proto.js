{
  streets: [{
    id: Number,
    // -1 for left or up 1 for right or down
    dir: -1 || 1,
    startX: Number,
    startY: Number,
    endX: Number,
    endY: Number
  }],
  sensors: [{
    id: Number,
    x: Number,
    y: Number
  }]
}
